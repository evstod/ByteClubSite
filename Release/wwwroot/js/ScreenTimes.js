//Need a formula to convert (TimeSpan)Agenda.StartTime into Date
debugger;
var starttimes;
for (var i = 0; i < starttimes.length; i++) {
    starttimes[i] = JSON.parse(@Model.AgendasList);
    console.log(starttimes[i]);
}
